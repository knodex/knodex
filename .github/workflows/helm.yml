name: Helm Chart

on:
  pull_request:
    paths:
      - 'deploy/charts/**'
      - '.github/configs/ct-*.yaml'
      - '.github/configs/lintconf.yaml'
      - '.github/workflows/helm.yml'

permissions:
  contents: read

jobs:
  chart-lint-test:
    name: Lint & Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Set up Helm
        uses: azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310 # v5.0.1
        with:
          version: v3.17.0

      - name: Add Helm repos
        run: |
          helm repo add bitnami https://charts.bitnami.com/bitnami
          helm repo update

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.12"

      - name: Set up chart-testing
        uses: helm/chart-testing-action@6ec842c01de15ebb84c8627d2744a0c2f2755c9f # v2.8.0

      - name: List changed charts
        id: list-changed
        run: |
          changed=$(ct --config ./.github/configs/ct-lint.yaml list-changed)
          if [[ -n "$changed" ]]; then
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Run chart-testing (lint)
        run: ct lint --config ./.github/configs/ct-lint.yaml --lint-conf ./.github/configs/lintconf.yaml

      - name: Run helm template
        run: |
          helm dependency build deploy/charts/knodex
          helm template knodex deploy/charts/knodex --values deploy/charts/knodex/values.yaml > /dev/null

      - name: Verify resource-wrappers ConfigMap renders
        run: |
          set -euo pipefail
          # Default values: ConfigMap rendered unconditionally with empty array.
          rendered_default=$(helm template knodex deploy/charts/knodex --show-only templates/resource-wrappers-configmap.yaml)
          if ! echo "$rendered_default" | grep -q "name: knodex-resource-wrappers"; then
            echo "::error::default render did not produce the knodex-resource-wrappers ConfigMap"
            echo "$rendered_default"
            exit 1
          fi
          if ! echo "$rendered_default" | grep -q "knodex.io/config-type: resource-wrappers"; then
            echo "::error::default render missing knodex.io/config-type label"
            exit 1
          fi
          # With a Project wrapper configured, the rendered JSON must contain the rgdName.
          rendered_with=$(helm template knodex deploy/charts/knodex \
            --show-only templates/resource-wrappers-configmap.yaml \
            --set 'server.project.wrapperRGD=wrapped-project-v1')
          if ! echo "$rendered_with" | grep -q "wrapped-project-v1"; then
            echo "::error::wrapper render missing wrapped-project-v1"
            echo "$rendered_with"
            exit 1
          fi
          if ! echo "$rendered_with" | grep -q '"kind"'; then
            echo "::error::wrapper render missing kind field"
            echo "$rendered_with"
            exit 1
          fi

      - name: Verify LOCAL_LOGIN_ENABLED env injection (default + disabled fixture)
        run: |
          set -euo pipefail
          # Default values: LOCAL_LOGIN_ENABLED must render as "true"
          rendered_default=$(helm template knodex deploy/charts/knodex)
          if ! echo "$rendered_default" | grep -A1 "LOCAL_LOGIN_ENABLED" | grep -q '"true"'; then
            echo "::error::default render did not emit LOCAL_LOGIN_ENABLED=\"true\""
            echo "$rendered_default" | grep -A1 "LOCAL_LOGIN_ENABLED" || true
            exit 1
          fi
          # Disabled fixture: must render as "false" and the deployment must NOT
          # reference the local login route handler.
          rendered_disabled=$(helm template knodex deploy/charts/knodex \
            -f deploy/charts/knodex/ci/values-local-login-disabled.yaml)
          if ! echo "$rendered_disabled" | grep -A1 "LOCAL_LOGIN_ENABLED" | grep -q '"false"'; then
            echo "::error::disabled fixture did not emit LOCAL_LOGIN_ENABLED=\"false\""
            echo "$rendered_disabled" | grep -A1 "LOCAL_LOGIN_ENABLED" || true
            exit 1
          fi

      # Install testing is skipped on PRs because the Docker image for the
      # chart version doesn't exist yet in the registry. The E2E workflow
      # provides full deployment validation with locally-built images.
      # Lint + helm template above catch chart configuration issues.
