apiVersion: v1
kind: ConfigMap
metadata:
  name: knodex-category-config
  namespace: knodex-system
data:
  categories: |
    - name: Infrastructure
      weight: 10
      icon: server
    - name: Applications
      weight: 20
    - name: Networking
      weight: 30
      icon: network
