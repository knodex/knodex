{{- if .Values.dex.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "knodex.fullname" . }}-dex-server
  labels:
    {{- include "knodex.dex.labels" . | nindent 4 }}
{{- end }}
